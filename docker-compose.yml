services:
  db:
    image: mysql:8.0
    container_name: mysql-db
    environment:
      MYSQL_DATABASE: damage_manager_db
      MYSQL_ROOT_PASSWORD: sorina
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  backend:
    build: ./vehicle-damage-manager
    container_name: spring-backend
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/damage_manager_db?allowPublicKeyRetrieval=true&useSSL=false
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=sorina
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update

  frontend:
    build: ./vehicle-damage-manager-ui
    container_name: react-frontend
    ports:
      - "3000:5173" # Mapam portul de dev al Vite
    volumes:
      - ./vehicle-damage-manager-ui:/app # "Leaga" folderul local de cel din Docker
      - /app/node_modules # Protejeaza modulele instalate in container
    environment:
      - CHOKIDAR_USEPOLLING=true # O alta metoda de a forta refresh-ul
    depends_on:
      - backend